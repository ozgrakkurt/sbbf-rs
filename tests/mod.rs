use sbbf_rs::FilterFn;

#[test]
fn test_filter() {
    let filter_fn = FilterFn::new();
    dbg!(filter_fn.which());
    let len = 3 * 65536;
    let len = ((number + multiple / 2) / multiple) * multiple;
}

struct Buf {}
